# Dashboard Results

The dashboard reads completed inspection runs from `dashboard/results/runs/`.

Each run should contain a `results.json` file plus optional artifact files referenced inside that JSON.
Generated artifacts inside run subfolders are ignored by Git.
